# Что не должно попадать в образ.
# Первый блок — про секреты: файл окружения, случайно оказавшийся в каталоге
# фронтенда, уехал бы вместе с образом в реестр. Vite вшивает VITE_*-переменные
# прямо в собранный js, поэтому здесь это особенно важно.
.env
.env.*
secrets/

node_modules
dist
.npm-cache
.playwright-mcp
.qa-shots
tsconfig.tsbuildinfo
*.log
.DS_Store
